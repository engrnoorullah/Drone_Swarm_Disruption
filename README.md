This code is based on multiprocessor run.
It simulates a drone swarm's motion from point A to point B.
During the swarm's motion, various disruption policies are applied to disrupt the swarm by removing nodes/drones from the network.
Finally all the policies are compared against each other.
There is also a code where a small set of drones is used to analyze the optimal solution.
